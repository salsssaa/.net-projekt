# Environment Variables Template

## Backend (.NET)
# Connection string is in appsettings.json
# You can override it with environment variable:
# ConnectionStrings__DefaultConnection=Host=localhost;Port=5432;Database=productdb;Username=postgres;Password=postgres123

## Frontend (React)
# API URL - change if backend runs on different port
VITE_API_URL=http://localhost:5000/api
